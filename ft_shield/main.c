#include <stdio.h>
#include "payload_info.h"

int main(int argc, char const *argv[])
{
	// TODO: check sudo
	printf("hello shield: %s\n", PAYLOAD_BYTES);


	// TODO: copy the binary in the place where daemon binaries are stored..

	// TODO: launch the daemon, maybe also make it launch on startup

	// TODO: run legit
}
